import React from 'react'

const Button = () => {
    return (
        <div>
            <button>I am a Button component and child component of PrintMyNameComponent</button>
        </div>
    )
}

export default Button;